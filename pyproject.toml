[project]
name = "minimal-self-harness"
version = "0.1.0"
description = "A minimal runnable Self-Harness demo loop with pytest-based validators."
requires-python = ">=3.11"
dependencies = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
addopts = "-q"
